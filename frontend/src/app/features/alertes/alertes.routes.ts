import { Routes } from '@angular/router';

import { AlertesComponent } from './pages/alertes/alertes.component';

export const ALERTES_ROUTES: Routes = [{ path: 'alertes', component: AlertesComponent }];